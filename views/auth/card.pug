div(class="bg-white shadow rounded-lg")
                        img(class="object-cover h-72 w-full" src=`/uploads/${restaurante.imagen}` alt=`Agregen productos ${restaurante.imagen}`)
                        div(class="p-5 space-y-3")
                            h2(class="text-2xl font-bold")=restaurante.titulo
                                p(class="text-sm text-gray-600")= restaurante.precio.nombre
                            a(href=`/productos/${restaurante.id}` class="bg-yellow-600 w-full text-center block font-bold text-white p-2 uppercase rounded text-2xl") Ver producto
